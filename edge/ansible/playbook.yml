---
- name: Configure Raspberry Pi Sculpture Nodes
  hosts: sculptures
  become: yes
  vars:
    sculpture_dir: /opt/sculpture-system

  tasks:
    - name: Update package cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Upgrade installed packages
      apt:
        upgrade: dist

    - name: Install required packages
      apt:
        name:
          - darkice
          - mpv
          - python3-paho-mqtt
          - pulseaudio
          - rsync
          - alsa-utils
          - python3-pip
          - git
        state: present

    - name: Disable WiFi power saving
      copy:
        dest: /etc/NetworkManager/conf.d/wifi-powersave.conf
        content: |
          [connection]
          wifi.powersave = 2
        owner: root
        group: root
        mode: '0644'
      notify: Restart NetworkManager

    - name: Detect firmware config path
      stat:
        path: /boot/firmware/config.txt
      register: firmware_cfg

    - name: Set boot config path
      set_fact:
        boot_cfg: >-
          {{ '/boot/firmware/config.txt'
             if firmware_cfg.stat.exists
             else '/boot/config.txt' }}

    - name: Enable IQaudIO codec overlay
      lineinfile:
        path: "{{ boot_cfg }}"
        regexp: '^dtoverlay=iqaudio-codec'
        line: 'dtoverlay=iqaudio-codec'
      register: overlay

    - name: Enable I2S for audio HAT
      lineinfile:
        path: "{{ boot_cfg }}"
        regexp: '^#? *dtparam=i2s=on'
        line: 'dtparam=i2s=on'
        state: present
      register: i2s_on

    - name: Remove conflicting dtparam=audio=off to prevent I2S sync errors
      lineinfile:
        path: "{{ boot_cfg }}"
        regexp: '^dtparam=audio=off'
        state: absent
      register: audio_off

    - name: Reboot if audio configuration changed
      reboot:
        reboot_timeout: 300
      when: overlay.changed or i2s_on.changed or audio_off.changed

    - name: Clone Pi-Codec repository
      git:
        repo: https://github.com/raspberrypi/Pi-Codec.git
        dest: "{{ sculpture_dir }}/Pi-Codec"
        version: master

    - name: Create sculpture system directory
      file:
        path: "{{ sculpture_dir }}"
        state: directory
        mode: '0755'

    - name: Ensure ffmpeg is installed on the control node (using apt for Debian/Ubuntu)
      local_action:
        module: ansible.builtin.apt
        name: ffmpeg
        state: present
        update_cache: yes
      become: yes
      become_method: sudo
      run_once: true # We only need to check this once

    - name: Create a temporary local directory for converted loops
      local_action:
        module: ansible.builtin.file
        path: /tmp/converted_loops
        state: directory
      become: no
      run_once: true

    - name: Convert loop files to the correct format locally
      local_action:
        module: ansible.builtin.shell
        cmd: 'for f in ../../samples/*.wav; do ffmpeg -y -i "$f" -ac {{ audio_channels }} -ar {{ audio_sample_rate }} "/tmp/converted_loops/$(basename "$f")"; done'
      become: no
      run_once: true

    - name: Copy the CONVERTED loop files to the edge device
      ansible.builtin.copy:
        src: /tmp/converted_loops/
        dest: /opt/sculpture-system/
        owner: pi
        group: audio
        mode: '0644'

    - name: Clean up temporary local directory
      local_action:
        module: ansible.builtin.file
        path: /tmp/converted_loops
        state: absent
      become: no
      run_once: true

    - name: Template darkice configuration
      template:
        src: ../darkice/darkice.cfg
        dest: "{{ sculpture_dir }}/darkice.cfg"
        mode: '0644'

    - name: Create sculpture config directory
      file:
        path: /etc/sculpture
        state: directory
        mode: '0755'

    - name: Template pi-agent audio configuration
      template:
        src: templates/audio.conf.j2
        dest: /etc/sculpture/audio.conf
        mode: '0644'
      notify: Restart pi-agent

    - name: Configure PulseAudio for the target sample rate
      lineinfile:
        path: /etc/pulse/daemon.conf
        regexp: "{{ item.regexp }}"
        line: "{{ item.line }}"
        state: present
      with_items:
        - { regexp: '^; default-sample-rate = .*', line: 'default-sample-rate = {{ audio_sample_rate }}' }
        - { regexp: '^; alternate-sample-rate = .*', line: 'alternate-sample-rate = {{ audio_sample_rate }}' }
      notify: Restart pulseaudio

    - name: Copy pi-agent script
      copy:
        src: ../scripts/pi-agent.py
        dest: "{{ sculpture_dir }}/pi-agent.py"
        mode: '0755'
      notify: Restart pi-agent

    - name: Install darkice systemd service
      template:
        src: ../systemd/darkice.service.j2
        dest: /etc/systemd/system/darkice.service
        owner: root
        group: root
        mode: '0644'
      notify: Restart darkice

    - name: Install player-live systemd service
      template:
        src: ../systemd/player-live.service.j2
        dest: /etc/systemd/system/player-live.service
        owner: root
        group: root
        mode: '0644'
      notify: Restart player-live

    - name: Install player-loop systemd service
      template:
        src: ../systemd/player-loop.service.j2
        dest: /etc/systemd/system/player-loop.service
        owner: root
        group: root
        mode: '0644'
      notify: Restart player-loop

    - name: Install pi-agent systemd service
      template:
        src: ../systemd/pi-agent.service.j2
        dest: /etc/systemd/system/pi-agent.service
        owner: root
        group: root
        mode: '0644'
      notify: Restart pi-agent

    - name: Enable and start services
      systemd:
        name: "{{ item }}"
        enabled: yes
        state: started
        daemon_reload: yes
      loop:
        - darkice.service
        - player-live.service
        - pi-agent.service

    - name: Ensure player-loop is disabled by default
      systemd:
        name: player-loop.service
        enabled: no
        state: stopped

    - name: Create dedicated PulseAudio sink for the sculpture
      lineinfile:
        path: /etc/pulse/default.pa
        regexp: '^load-module module-null-sink sink_name=sculpture_sink'
        line: 'load-module module-null-sink sink_name=sculpture_sink sink_properties=device.description=Sculpture_Sink channel_map={{ "mono" if audio_channels == 1 else "stereo" }}'
        state: present
      notify: Restart pulseaudio

    - name: Ensure pi-agent service is started and enabled
      systemd:
        name: pi-agent.service
        enabled: yes
        state: started

    - name: Grant pi-agent limited sudo access for service management
      template:
        src: templates/pi-agent-sudoers.j2
        dest: "/etc/sudoers.d/pi-agent-sudo"
        owner: root
        group: root
        mode: "0440"
        validate: 'visudo -cf %s'

  handlers:
    - name: Restart pulseaudio
      shell: "pulseaudio -k"
      become: yes
      become_user: pi
      listen: "Restart pulseaudio"

    - name: Reboot for audio config
      reboot:
        msg: "Rebooting to apply audio configuration changes"
        reboot_timeout: 3600

    - name: Restart darkice
      systemd:
        daemon_reload: yes
        name: darkice.service
        state: restarted

    - name: Restart player-live
      systemd:
        daemon_reload: yes
        name: player-live.service
        state: restarted

    - name: Restart player-loop
      systemd:
        daemon_reload: yes
        name: player-loop.service
        state: restarted

    - name: Restart pi-agent
      systemd:
        daemon_reload: yes
        name: pi-agent.service
        state: restarted

    - name: Restart NetworkManager
      systemd:
        name: NetworkManager.service
        state: restarted
